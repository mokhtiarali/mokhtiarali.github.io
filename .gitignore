# Agent instruction files — local only, never pushed and never published.
# Also listed in _config.yml `exclude:` so they stay out of _site -> gh-pages.
AGENTS.md
CLAUDE.md
.claude/
.github/agents/
.github/instructions/
.github/copilot-instructions.md

_site
.bundle
.DS_store
.idea
.jekyll-cache
.jekyll-metadata
.ruby-version
.sass-cache
.tweet-cache
.venv
assets/libs/
assets/rendercv/rendercv_output/*.typ
Gemfile.lock
node_modules/
vendor